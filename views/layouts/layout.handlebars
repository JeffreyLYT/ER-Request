<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>(ER)equest</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <style>
        .container {
            width: 100%;
        }
    </style>
</head>
	<body>
		<div class="container-fluid">
            <div class="header clearfix">
                <nav>
                    <h3 class="text-muted" style="display:inline-block;">(<strong>ER</strong>)equest</h3>
                    <ul class="nav nav-pills pull-right">
                        {{#if user}}

                        <li role="presentation"><a href="/"><span class="glyphicon glyphicon-comment"></span> Dashboard</a></li>
                        <li role="presentation"><a href="/requests/new"><span class="glyphicon glyphicon-plus"></span> New e-Request</a></li>
                        <li role="presentation"><a href="/requests"><span class="glyphicon glyphicon-pencil"></span> Your Requests</a></li>
                        {{#if user.admin}}
                        <li role="presentation"><a href="/userDB"><span class="glyphicon glyphicon-hdd"></span> User Database</a></li>


                        {{/if}}
                        <li role="presentation"><a href="/fitbit/register"><span class="glyphicon glyphicon-heart"></span> Link FitBit Account</a></li>
                        <li role="presentation"><a href="/users/logout"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>

                        {{else}}

                        <li role="presentation"><a href="/users/login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                        <li role="presentation"><a href="/users/register"><span class="glyphicon glyphicon-user"></span> Register</a></li>

                        {{/if}}
                    </ul>
                </nav>

            </div>
      

     
      <div class="col-xl-12">
        {{#if success_msg}}
        	<div class="alert alert-success">{{success_msg}}</div>
       	{{/if}}

       	{{#if error_msg}}
        	<div class="alert alert-danger">{{error_msg}}</div>
       	{{/if}}

       	{{#if error}}
        	<div class="alert alert-danger">{{error}}</div>
       	{{/if}}
          <div style="width: 90%;margin-left: auto;margin-right: auto;">
            {{{body}}}
          </div>
        <br><br>
      </div>

      <div class="col-xl">
        <footer class="footer">
            <p>&copy; 2019 (ER)equest Team</p>
        </footer>
      </div>
    </div>
	</body>
</html>